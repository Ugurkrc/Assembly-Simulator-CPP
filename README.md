# Assembly-Simulator-CPP
A low-level virtual CPU and memory simulator written in C++ from scratch. Implements a custom instruction set architecture (ISA) with manual string parsing and memory management, without relying on standard tokenization libraries.
